One asset per interactable object type. Create via: Right-click → Create → Game → Interactable Data.
Each asset references three (or four) DialogueData assets — one per room state.
  e.g. InteractableData_Lamp.asset, InteractableData_Rug.asset
